version: "3.9"

services:
  fastapi:
    build: .
    container_name: fastapi-ai-backend
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./sqlite.db:/app/sqlite.db
    restart: unless-stopped